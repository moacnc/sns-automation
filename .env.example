# 환경변수 설정 파일 예시
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값으로 변경하세요
# cp .env.example .env

# ===== PostgreSQL / AlloyDB 데이터베이스 설정 =====

# 데이터베이스 호스트
# - 로컬 개발: localhost
# - AlloyDB: Private IP 또는 Public IP
DB_HOST=localhost

# 데이터베이스 포트
DB_PORT=5432

# 데이터베이스 이름
DB_NAME=instagram_automation

# 데이터베이스 사용자
DB_USER=postgres

# 데이터베이스 비밀번호 (필수)
# ⚠️ 절대 Git에 커밋하지 마세요!
DB_PASSWORD=your_secure_password_here

# SSL 모드
# - prefer: SSL을 선호하지만 필수 아님
# - require: SSL 필수 (AlloyDB 권장)
# - disable: SSL 비활성화 (개발 환경만)
DB_SSLMODE=prefer

# ===== AlloyDB 설정 (선택사항) =====

# AlloyDB 인스턴스 연결 이름
# ALLOYDB_INSTANCE=projects/PROJECT_ID/locations/REGION/clusters/CLUSTER_ID/instances/INSTANCE_ID

# AlloyDB Public IP 사용 시
# ALLOYDB_USE_PUBLIC_IP=true

# ===== 이메일 알림 설정 (선택사항) =====

# SMTP 서버 비밀번호
# EMAIL_PASSWORD=your_email_password

# ===== OpenAI API 설정 =====

# OpenAI API Key (필수)
# ⚠️ 절대 Git에 커밋하지 마세요!
# https://platform.openai.com/api-keys 에서 발급
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Organization ID (선택사항)
# OPENAI_ORG_ID=your_org_id

# ===== 기타 설정 =====

# 환경 (development, production)
ENVIRONMENT=development

# 로그 레벨 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
